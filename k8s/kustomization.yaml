apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: files-management
  namespace: files-management

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - cluster-issuer.yaml

namespace: files-management

images:
  - name: ghcr.io/rxtech-lab/files-management
    newTag: latest
